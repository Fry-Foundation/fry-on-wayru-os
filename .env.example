# Fry IoT Build Configuration
# Copy this file to .env and customize as needed

# Device profile to build (see profiles/ directory)
PROFILE=x86-generic

# Azure Blob Storage configuration (for upload-build)
AZURE_CONNECTION_STRING=""
CONTAINER_NAME="fry-iot-images"

# Optional: Override image size (default from profile)
# IMAGE_SIZE=4G

# Optional: Enable verbose build output
# DEBUG=1
